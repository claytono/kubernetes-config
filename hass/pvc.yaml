---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: hass-config
  labels:
    k8s-app: hass-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
