---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kibana
  namespace: kube-system
  annotations:
    kubernetes.io/ingress.class: "nginx"
    external-dns.alpha.kubernetes.io/external-ip: "172.19.74.20"
spec:
  rules:
    - host: "kibana.k.oneill.net"
      http:
        paths:
          - path: /
            backend:
              serviceName: kibana-logging
              servicePort: 5601
