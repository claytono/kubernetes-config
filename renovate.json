{
  "description": "Renovate config for Helm chart version automation in chart-versions.yaml.",
  "extends": ["config:base"],
  "packageRules": [
    {
      "matchDatasources": ["helm"],
      "matchPackagePatterns": [".*"]
    }
  ],
  "yaml": {
    "fileMatch": ["^chart-versions.yaml$"]
  }
}
